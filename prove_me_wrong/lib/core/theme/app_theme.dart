import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // private constructor

  static const primary = Color(0xffa31e1e);          // ana kırmızı
  static const onPrimary = Color(0xffede3d7);        // background, card içi yazı
  static const primaryContainer = Color(0xff891b1b); // card içi yazı arka plan, koyu kırmızı

  static const secondary = Color(0xff6b2f2f);        // random ve signup sayfası -- en koyu kırmızı
  static const onSecondary = Color(0xfff7eaea);      // kategori içi yazı -- açık pembe

  static const tertiary = Color(0xffd87070);         // room oluşturma, menü bar highligtlama -- pembe
}